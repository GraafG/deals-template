# Copilot instructions

Read `AGENTS.md` before making changes. This is the canonical shared provider template; keep changes reusable and avoid provider-specific hard-coding unless it belongs in example provider config/data.
